# Practica Flujo Basico en git y github
En esta pracatica realizamos un repositorio en el que clonamos,pusimos descripcion desde python y pusimos la descripcion de lo que se hizo el dia de hoy,en este repositorio utilizaremos varios comando como por ejemplo:
dir
git clone
git status
git add .
git commit -m "cambiaremos la descripcion"
Al final fue una practica en donde la realizamos para entregarla al profesor.

